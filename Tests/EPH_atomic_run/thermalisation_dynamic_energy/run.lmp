
# initialisation
units metal
# mass -> grams/mole
# distance -> Angstroms (A)
# time -> picoseconds (ps)
# energy -> eV
# velocity -> A/ps
# force -> eV/A
# torque -> eV
# temperature -> K
# pressure -> bars
# dynamic viscosity -> Poise
# charge -> e
# dipole -> e*A
# electric field -> V/A
# density -> gram/cm^3

atom_style atomic

boundary p p p
atom_modify map array

######################
### create the box ###
######################

## temperature ##
variable T equal 300.0
variable T2 equal 2*${T}

## box size ##
variable n equal 8
variable n_2 equal floor(${n}/2)

## lattice constant ##
variable a equal 3.62

variable x index $n
variable y index $n
variable z index $n

## lattice type ##
lattice fcc $a
region box block 0 $x 0 $y 0 $z

## create box for two atom types ##
create_box 1 box
## fill box with atoms ##
create_atoms 1 box

## potential in use ##
pair_style eam/alloy
pair_coeff * * Cu.eam Cu

#############################
### intergration timestep ###
#############################
timestep 1e-5

##################################
### Create initial temperature ###
##################################
velocity all create ${T2} 101 mom yes rot yes

################################
### Integrator and eph model ###
################################

## use velocity verlet integration 
#fix integrate all nve

## eph model ##
## model is applied only to the PKA atom (a small error is created because of this) ##
fix friction all eph/atomic 1111 7 ${T2} NULL 1 NULL Cu.beta Cu.kappa Cu
run 0

##############
### Output ###
##############
## print stuff into stdout every 1000 steps (atoms moves about 1A in that time) ##
thermo_style custom step time temp press pe ke etotal f_friction[1] f_friction[2]
thermo 100

##########################
### run the simulation ###
##########################
run 100000

